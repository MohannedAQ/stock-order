{
  "outputDirectory": "client/dist",
  "framework": "vite",
  "buildCommand": "vite build --config client/vite.config.ts",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
